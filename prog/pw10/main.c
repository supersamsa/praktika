#include "stringfunc.h"

int main(int argc, char argv[]) {

  char *buffer = readInput();
  countChars(buffer);
  countSecWordsChars(buffer);
  printf("words %d", countInputWordLen(char buff[], int length)); 
  return 0;
}